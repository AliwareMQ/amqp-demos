public class Config {
    protected static final String HOSTNAME = "your-instance-endpoint"; // 替换为配置的阿里云MQ实例的公网接入点
    protected static final String USERNAME = "your-username"; // 替换为配置的静态用户名
    protected static final String PASSWORD = "your-password"; // 替换为配置的静态密码
}
